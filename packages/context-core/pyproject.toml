[project]
name = "context-med-core"
version = "0.1.0"
description = "Context-Med Orchestrator"
dependencies = [
    "typer>=0.12.0",
]

[project.scripts]
context-med = "context_core.cli:app"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
